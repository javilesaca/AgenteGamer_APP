<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para acceder a Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AgenteGamerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgenteGamer">
        <!--Activity launcher de login -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"/>
        <!-- Activity principal -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AgenteGamer">
        </activity>
        <!-- Activity de gastos -->
        <activity
            android:name=".ui.gastos.ListaGastosActivity"
            android:exported="false"
            android:theme="@style/Theme.AgenteGamer">
        </activity>
        <!-- Activity para ver juegos -->
        <activity
            android:name=".ui.games.ListaJuegosActivity"
            android:exported="false">
        </activity>
        <!-- Activity para ver lista de juegos deseados -->
        <activity
            android:name=".ui.wishlist.ListaWishlistActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".ui.lanzamientos.LanzamientosActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".ui.perfil.PerfilActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".ui.ajustes.AjustesActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>